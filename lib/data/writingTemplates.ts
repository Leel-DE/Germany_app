import type { WritingTemplate } from "@/types";

// Legacy export kept for older imports. Database seed data in seedWriting.ts is
// the source of truth for Writing tasks.
export const WRITING_TEMPLATES: WritingTemplate[] = [];
